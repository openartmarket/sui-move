export SUI_NETWORK="localnet"
export SUI_VERSION="1.14.0"
# Sometimes the Sui team doesn't tag their releases consistently
# Check git tags on https://github.com/MystenLabs/sui to find the appropriate rev (tag)
# IMPORTANT!! Keep this in sync with Sui.rev in Move.toml
export SUI_GIT_TAG="testnet-v1.14.0"

source_env '.sui.env'